# Copy to .env for local dev. Render: set these as environment variables in the dashboard.

# Comma-separated allowed CORS origins. Local dev defaults to http://localhost:3000 if unset.
ALLOWED_ORIGINS=http://localhost:3000,https://medsafe-nine.vercel.app
